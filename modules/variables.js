/**
 * Variables
 */

var hyper = HYPER_KEYS
var hyperShift = [...HYPER_KEYS, ...HYPER_SHIFT]

var mousePositions = {} // mouse position for window
var activeWindowsTimes = {} // last active time for window
